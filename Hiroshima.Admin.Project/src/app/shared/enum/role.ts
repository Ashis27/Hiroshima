/**
 * Role based enum
 */
export enum Role {
    /**
     * Role == Admin
     */
    Admin = "Admin",
     /**
     * Role == SuperAdmin
     */
    SuperAdmin = "SuperAdmin",
     /**
     * Role == NECAdmin
     */
    NECAdmin = "NECAdmin",
     /**
     * Role == User
     */
    User = "User",
}